export enum Role {
    ADMIN = "admin",
    STUDENT = "student",
}

export enum isPaid {
    YES = "yes",
    NO = "no"   
}

export enum LEVEL {
    one = "one",
    two = "two",
    three = "three"
}